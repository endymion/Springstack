# Springstack BDD Specs

This folder will contain Gherkin feature files that define Springstack behavior.

Run the BDD suite:

```bash
npm test
```

Scenarios tagged with `@skip` are intentionally excluded until implemented.
