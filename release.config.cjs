module.exports = {
  branches: ['main'],
  plugins: [
    ['@semantic-release/commit-analyzer', { preset: 'conventionalcommits' }],
    ['@semantic-release/npm', { pkgRoot: 'packages/springstack', npmPublish: false }],
    [
      '@semantic-release/exec',
      {
        generateNotesCmd: "node -e \"console.log('Release notes are currently generated by semantic-release without commit dates.');\"",
        publishCmd: 'cd packages/springstack && npm publish --access public --provenance'
      }
    ],
    '@semantic-release/github'
  ]
};
